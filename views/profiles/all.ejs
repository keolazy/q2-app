<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>ReadMe App</title>
    <link href="https://fonts.googleapis.com/css?family=Dosis" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.10/css/all.css" integrity="sha384-+d0P83n9kaQMCwj8F4RJB66tzIwOKmrdb46+porD/OvrJ+37WqIM7UoBtwHO6Nlg" crossorigin="anonymous">
  </head>
  <body>
    <header id="header">
      <span id="title">Readme App</span>
      <span id="hamburger-menu"><i class="fas fa-bars"></i></span>
      <nav id="nav" class="nav__hidden">
        <button id="nav__close-button" type="button" name="button">X</button>
        <ul id="nav__ul">
        </ul>
      </nav>
    </header>
    <main id="main">
      <section class="section">
        <div class="section__title">Event: <%= event.name %></div>
        <div class="section__subtitle">Here's who's attending:</div>
        <!-- <p class="section__text">
          We'll use this to check if you have an active session.
        </p> -->
        <% profiles.forEach(profile => { %>
          <div class="card">
            <div class="card__title"><%= `${profile.first} ${profile.last[0]}.` %></div>
            <ul class="card__list">
              <% for (let key=3; key < Object.keys(profile).length; key++) { %>
              <li>
                  <span class="card__prop"><%= Object.keys(profile)[key] %>:</span>
                  <%= profile[Object.keys(profile)[key]] %>
              </li>
              <% } %>
            </ul>
            <div class="card__buttons">
              <button class="card__button card__button--green"><a href="/connections/<%= profile.id %>_method=POST">Ask to Connect</a></button>
              <button class="card__button card__button--red">Cancel Connection</button>
            </div>
          </div>
        <% }) %>
      </section>
    </main>
    <script src="/shared_scripts.js" charset="utf-8"></script>
  </body>
</html>
